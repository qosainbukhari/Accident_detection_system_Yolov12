Accident detection model bundle

Classes: 0=fire, 1=moderate, 2=severe
Model file: best.pt

Install the model locally with:
cp best.pt backend/ml_model/best.pt

The backend must use the same class order.
